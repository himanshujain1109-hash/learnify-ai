import {useState} from "react";import {useParams} from "react-router-dom";import {askTutor} from "../services/tutor";
export default function Tutor(){const{documentId}=useParams();const[question,setQuestion]=useState(""),[answer,setAnswer]=useState(""),[loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();if(!question.trim())return;setLoading(true);try{const d=await askTutor(documentId,question);setAnswer(d.answer)}catch(err){setAnswer(err.response?.data?.message||"Tutor failed")}finally{setLoading(false)}};return <div><div className="section-heading compact"><div><span className="eyebrow">YOUR MATERIAL, YOUR QUESTIONS</span><h2>AI Tutor</h2><p>Ask for explanations, examples or quick revision based on your uploaded material.</p></div></div><form onSubmit={submit} className="content-card"><div className="field"><label>What would you like to understand?</label><textarea value={question} onChange={e=>setQuestion(e.target.value)} placeholder="Explain this topic in simple language..."/></div><button className="btn btn-primary" disabled={loading}>{loading?"Thinking...":"Ask Learnify AI →"}</button></form>{answer&&<div className="content-card tutor-answer"><span className="eyebrow">AI TUTOR</span><h2>Teacher-style explanation</h2><div className="lesson">{answer}</div></div>}</div>}
