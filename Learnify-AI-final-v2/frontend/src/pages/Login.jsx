import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import {loginUser} from "../services/auth";import {useAuth} from "../context/AuthContext";
export default function Login(){const[form,setForm]=useState({email:"",password:""}),[error,setError]=useState("");const{login}=useAuth(),navigate=useNavigate();const submit=async e=>{e.preventDefault();setError("");try{login(await loginUser(form));navigate("/dashboard")}catch(err){setError(err.response?.data?.message||"Login failed")}};return <form className="form" onSubmit={submit}><span className="eyebrow">WELCOME BACK</span><h1>Log in to Learnify.</h1><p className="form-intro">Continue your learning workspace.</p>{error&&<div className="error">{error}</div>}<div className="field"><label>Email</label><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></div><div className="field"><label>Password</label><input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Your password"/></div><button className="btn btn-primary">Log in →</button><p className="muted">New here? <Link className="card-link" to="/register">Create your account</Link></p></form>}
